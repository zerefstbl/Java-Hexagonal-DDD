mysql:
  username: ${DATABASE_MYSQL_USERNAME:root}
  password: ${DATABSAE_MYSQL_PASSWORD:123456}
  schema: adm_videos
  url: ${DATABASE_MYSQL_URL:localhost:localhost:3306}

spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
